# Set the API URL for the frontend (defaults to http://localhost:3000)
VITE_API_URL="http://localhost:3000"
GEMINI_API_KEY="your-gemini-api-key"

# Example: DATABASE_URL is used by the backend (deno/api-server) when running locally
# DATABASE_URL="postgresql://admin:admin@localhost:5432/error_log_streamer"
